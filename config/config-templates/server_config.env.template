# ── Identity ─────────────────────────────────────────────
# The short hostname of your Atlas server.
# Default = atlas
# This defines the first part of your service URLs.
# Example: if set to "hephaestus", Homepage will be http://hephaestus.lan
# Docs: https://en.wikipedia.org/wiki/Hostname
ATLAS_HOSTNAME=atlas

# The base domain for all services.
# Default = lan (suitable for LAN-only access with local DNS or Tailscale MagicDNS).
# Example: "home.arpa" or "local".
# Docs: https://tailscale.com/kb/1081/magicdns
BASE_DOMAIN=lan

# Fully qualified domain name base.
# Do not edit directly — it is auto-generated from HOSTNAME + BASE_DOMAIN.
# Example: atlas.lan
# Docs: https://en.wikipedia.org/wiki/Fully_qualified_domain_name
FQDN_BASE=${ATLAS_HOSTNAME}.${BASE_DOMAIN}


# ── Paths ────────────────────────────────────────────────
# Root path for persistent Atlas service data (volumes, configs).
# Default = /srv/atlas
# If you change this, you must migrate existing volumes.
# Docs: https://refspecs.linuxfoundation.org/FHS_3.0/fhs/ch03s17.html
DATA_ROOT=/srv/atlas

# Root path for backups.
# Default = /srv/atlas-backups
# Used by backup scripts to store snapshots.
# Docs: https://wiki.archlinux.org/title/Backup
BACKUPS_ROOT=/srv/atlas-backups


# ── Networking ───────────────────────────────────────────
# Ports exposed by Traefik (reverse proxy).
# Normally you should keep these at 80/443 unless you have conflicts.
# Docs: https://doc.traefik.io/traefik/routing/entrypoints/
TRAEFIK_HTTP_PORT=80
TRAEFIK_HTTPS_PORT=443

# Docker network used by all Atlas services.
# Default = atlas_net
# If changed, ensure all docker-compose.yml files reference the new name.
# Docs: https://docs.docker.com/network/
ATLAS_DOCKER_NETWORK=atlas_net


# ── Admin ────────────────────────────────────────────────
# Timezone for services (affects logs, cronjobs, etc.)
# Example: Europe/Paris, UTC, America/New_York
# Docs: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=Europe/Paris

# User ID (PUID) and Group ID (PGID) for container permissions.
# Default = 1000 (first user on most Linux distros).
# If you run Atlas as another user, update these values to match `id -u` and `id -g`.
# Docs: https://en.wikipedia.org/wiki/User_identifier
PUID=1000
PGID=1000
